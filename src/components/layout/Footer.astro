---
import { siteConfig } from '../../config/site';
import Icon from '../ui/Icon.astro';
---

<footer style="background: #0d0d0d;">
  <div class="container-site py-12">

    <!-- Top row: brand + socials -->
    <div
      class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-6 pb-10 mb-8"
      style="border-bottom: 1px solid rgba(244,244,240,0.08);"
    >
      <div>
        <a
          href="/"
          class="block mb-2 text-sm font-medium tracking-[0.15em] uppercase"
          style="color: #f4f4f0;"
        >
          {siteConfig.name}
        </a>
        <p class="text-xs leading-relaxed max-w-xs" style="color: rgba(244,244,240,0.38);">
          {siteConfig.footer.tagline}
        </p>
      </div>

      <div class="flex gap-4" style="color: rgba(244,244,240,0.38);">
        <a href={siteConfig.contacts.telegram} class="footer-social-icon" target="_blank" rel="noreferrer" aria-label="Telegram"><Icon name="telegram" size={18} /></a>
        <a href={siteConfig.contacts.whatsapp} class="footer-social-icon" target="_blank" rel="noreferrer" aria-label="WhatsApp"><Icon name="whatsapp" size={18} /></a>
        <a href={siteConfig.contacts.instagram} class="footer-social-icon" target="_blank" rel="noreferrer" aria-label="Instagram"><Icon name="instagram" size={18} /></a>
      </div>
    </div>

    <!-- Bottom row -->
    <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-3">
      <p class="text-xs" style="color: rgba(244,244,240,0.25);">
        © {siteConfig.year} {siteConfig.name}. Все права защищены.
      </p>
      <a
        href={siteConfig.footer.privacyHref}
        class="text-xs footer-social"
        style="color: rgba(244,244,240,0.25);"
      >
        Политика конфиденциальности
      </a>
    </div>

  </div>
</footer>

<style>
  .footer-social-icon {
    color: rgba(244, 244, 240, 0.38);
    display: inline-flex;
    transition: color 200ms ease;
  }
  .footer-social-icon:hover {
    color: rgba(244, 244, 240, 0.80);
  }
  .footer-social {
    transition: color 200ms ease;
  }
  .footer-social:hover {
    color: rgba(244, 244, 240, 0.70);
  }
</style>
